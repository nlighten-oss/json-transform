{
  "converts a JSON object to a YAML string and then parses it back to a JSON object (object form)": [
    "$$yamlparse:$$yaml:$"
  ],
  "converts a JSON object with a \"title\" key to a YAML string (object form)": [
    "$$yaml:$"
  ],
  "converts a JSON object with a nested \"title\" key to a YAML string (object form)": [
    "$$yaml:$"
  ]
}