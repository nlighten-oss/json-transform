{
  "transforms an object by mapping its properties to new property names and values, and using the value of a specific property as the input (object form)": {
    "$$transform": "$.item",
    "to": {
      "id": "##current.id",
      "map_foo": "##current.foo",
      "value": "$.value"
    }
  },
  "splits the first character of a string (object form)": [
    "$$transform(##current[0]):$$split:$"
  ]
}